--- Application Config ---

PORT=4000
JWT_SECRET="your-super-secret-key-change-this"

Set expiration in seconds (86400s = 1 day)

JWT_EXPIRES_IN_SECONDS="86400"

--- Database Config ---

This URL points to the Docker container we just defined

DATABASE_URL="postgresql://brandie:password@localhost:5432/brandie_dev"

--- Test Database Config ---

We will create this 'brandie_test' database inside the same container

DATABASE_URL_TEST="postgresql://brandie:password@localhost:5432/brandie_test"