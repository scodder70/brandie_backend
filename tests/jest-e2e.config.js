// /** @type {import('ts-jest').JestConfigWithTsJest} */
// const unitConfig = require('../jest.config');

// module.exports = {
//   ...unitConfig,
//   // Override the testRegex to ONLY look in the tests/e2e directory
//   testRegex: '(/tests/e2e/.*|(\\.|/)(test|spec))\\.ts$',
//   // E2E tests can take longer, so increase the timeout
//   testTimeout: 10000,
//   // Don't run E2E tests in parallel
//   maxWorkers: 1,
// };